/**
 * Re-export canonical formatting helpers from @unseen-gadget/lib
 */
export {
  formatBDT,
  formatBDTCompact,
  formatDate,
  formatShortDate,
  formatDateTime,
  formatRelativeDay,
  formatDateOrDash,
  savingsAmount,
  calculateDiscountPercent,
  slugify,
  truncateText,
  capitalize,
} from '@unseen-gadget/lib';