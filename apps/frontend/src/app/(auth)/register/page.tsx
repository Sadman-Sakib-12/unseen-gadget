import { RegisterForm } from '@/features/auth/components/register-form';

export const metadata = {
  title: 'Create Account - Unseen Gadget',
  description: 'Create your Unseen Gadget account',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
